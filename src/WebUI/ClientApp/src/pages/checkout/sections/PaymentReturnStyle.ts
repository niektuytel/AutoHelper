import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
    container: { marginBottom:"100px", minHeight:"70vh" },
    paper: { padding:"15px" },
    btn_box: { display: 'flex', justifyContent: 'flex-end', paddingTop:"20px" },
    btn: { marginTop: 3, marginLeft: 1 }
}))
