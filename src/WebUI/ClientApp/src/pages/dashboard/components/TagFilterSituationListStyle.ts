import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
    root: {
        flexGrow: 1
    },
    list_paper: {
        padding: 2,
        display: 'flex',
        flexDirection: 'column',
        height: "100%",
    }
}));
